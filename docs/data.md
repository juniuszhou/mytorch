# data

## DataLoader
DataLoader 工作原理：

每个 Epoch 开始时：
DataLoader 会先生成一个随机索引列表（所有样本的序号被随机打乱）。
例如：数据集有 1000 个样本 → 生成类似 [783, 45, 912, ..., 7] 的随机排列。

然后按这个打乱后的顺序 依次取出数据组成 batch。
一个 Epoch 结束：
所有 1000 个样本都会被取完一次（除非你设置 drop_last=True）。
下一个 Epoch 又会重新随机打乱一次。
→ 这样既保证了随机性，又保证了完整性。